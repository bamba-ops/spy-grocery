export class Store {
    constructor(id, created_at, name, image_url) {
        this.id = id;
        this.created_at = created_at;
        this.name = name;
        this.image_url = image_url;
    }
}